{
  "name": "fintech",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "seo": "node scripts/generate-seo-files.cjs",
    "prebuild": "npm run seo",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "git": "^0.1.5"
  }
}
